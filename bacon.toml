[jobs.run]
command = ["cargo", "run"]
need_stdout = true
on_change_strategy = "kill_then_restart"

[jobs.test]
command = ["cargo", "test", "--", "--nocapture"]
need_stdout = true
allow_failures = true
